import { defineConfig } from 'vite';
import ghPages from 'vite-plugin-gh-pages';

export default defineConfig({
  plugins: [ghPages()],
  base: '/EleVis/' // Replace with your GitHub repo name if using GitHub Pages
});
